let username: string;
let age = 36;
let count: any;
let regNo: string | number;

username = "Max";

function add1(a: number, b: number) {
  return a + b;
}

add1(10, 20);
// add("10", "20");

count = "5";
count = 5;
count = true;

regNo = "BC45";
regNo = 45;
